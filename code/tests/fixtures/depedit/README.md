# DepEdit scenario fixtures

Create one subdirectory per maintained scenario, using the same identifier as
its directory under `code/rules/depedit/`. Each fixture set should contain the
smallest `input.conllu` and `expected.conllu` files that demonstrate the intended
change, plus focused cases for exclusions or boundary conditions when needed.

Do not use full treebanks or generated corpus-wide output as fixtures.
